services:
  landing:
    build: .
    container_name: storyforge-landing
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
